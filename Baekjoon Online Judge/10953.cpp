#include <bits/stdc++.h>

using namespace std;

int main() {
    ios::sync_with_stdio(0); 
    cin.tie(0); 
    cout.tie(0);

    char c;
    
    int t;
    cin >> t;
    while(t--) {
        int a, b;
        cin >> a >> c >> b;
        cout << a + b << '\n';
    }
}